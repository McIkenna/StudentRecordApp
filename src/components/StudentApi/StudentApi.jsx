import React from 'react'


/*export const StudentApi = (data, history) => async dispatch => {
        const response = await axios.get(``)
        dispatch({
            payload: response.data
        })


}
*/